stages:
  - deploy

deploy-production:
  stage: deploy
  script:
    - ssh -o StrictHostKeyChecking=no $DEPLOY_USER@$DEPLOY_SERVER "
        cd $DEPLOY_PATH &&
        git pull &&
        docker compose down &&
        docker compose up -d
      "
  only:
    - main
